import { TableController } from "./controllers/TableController";

document.addEventListener('DOMContentLoaded', () => {
    const tableController = new TableController ('file','search-container');
})
